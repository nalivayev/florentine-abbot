"""(Removed) Legacy validator module.

The filename validation logic has been moved to :mod:`common.naming` as
``FilenameValidator``. This file is intentionally left nearly empty to
avoid carrying duplicate logic; do not import from here.
"""

# Intentionally no public API here.
